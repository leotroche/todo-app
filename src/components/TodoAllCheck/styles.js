import styled from 'styled-components'

const TodoAllCheck = styled.section`
  width: 100%;
  display: flex;
  justify-content: space-between;

  button {
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    display: inline;
    cursor: pointer;
  }
`
export { TodoAllCheck }
